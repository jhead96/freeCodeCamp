// Properties can also be accessed using bracket notatation
// Properties with spaces must be accessed using bracket notation


const testObj = {
    "an entree": "hamburger",
    "my side": "veggies",
    "the drink": "water"
  };
  
  const entreeValue = testObj["an entree"];   
  const drinkValue = testObj["the drink"];   
