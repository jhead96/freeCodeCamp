// The ternery operator is a one line if else statement
// The syntax is a ? b : c
// a - condition, b - code to run when a is true, c - code to run when a is false

function checkEqual(a, b) {
    return a == b ? "Equal" : "Not Equal";
  }
  
  checkEqual(1, 2);