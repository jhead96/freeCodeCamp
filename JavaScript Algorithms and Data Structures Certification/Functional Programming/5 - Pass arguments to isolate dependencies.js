// In functional programming dependancies should be explicitly declared.

// The global variable
let fixedValue = 4;

// Only change code below this line
function incrementer(val) {
    return val + 1;
}
  // Only change code above this line