// Arguments can be passed into a constructor in the same way as functions

function Dog(name, color) {
    this.name = name;
    this.color = color;
    this.numLegs = 4;
  }
  
  let terrier = new Dog("Jake", "Black");